<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
    <style>
      body {
        font-family: Arial, Helvetica, sans-serif;
        background-color: #f8f9fa;
        margin: 0;
        padding: 20px;
        color: #333;
      }
      .container {
        max-width: 600px;
        margin: auto;
        background: #ffffff;
        border-radius: 8px;
        padding: 20px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      }
      h2 {
        color: #0066cc;
        margin-top: 0;
      }
      .info {
        margin: 10px 0;
        padding: 10px;
        border-bottom: 1px solid #eee;
      }
      .info strong {
        display: inline-block;
        width: 120px;
        color: #444;
      }
      .message {
        margin-top: 20px;
        padding: 15px;
        background: #f1f9ff;
        border-left: 4px solid #0066cc;
        border-radius: 4px;
        white-space: pre-line;
      }
      .footer {
        margin-top: 20px;
        font-size: 12px;
        color: #888;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h2>📩 Nouveau message depuis David Clim</h2>

      <div class="info"><strong>Nom :</strong> <%= @contact.last_name %></div>
      <div class="info"><strong>Téléphone :</strong> <%= @contact.phone_number %></div>
      <div class="info"><strong>Ville :</strong> <%= @contact.city %></div>
      <div class="info"><strong>Email :</strong> <%= @contact.email %></div>
      <div class="info"><strong>Consentement RGPD :</strong> <%= @contact.consent == "1" ? "✅ Oui" : "❌ Non" %></div>

      <div class="message">
        <%= @contact.message %>
      </div>

      <div class="footer">
        Cet email a été généré automatiquement par le site David Clim.
      </div>
    </div>
  </body>
</html>
